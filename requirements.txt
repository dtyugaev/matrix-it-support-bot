# --- Протокол Matrix ---
matrix-nio==0.25.2          # клиент Matrix (sync, комнаты, реакции, redact, загрузка медиа)

# --- Сеть и файлы ---
aiohttp==3.12.15            # асинхронные HTTP-запросы к Jira REST API и Synapse Admin API
aiofiles==24.1.0            # асинхронная запись/чтение файлов вложений

# --- Конфигурация ---
PyYAML==6.0.2               # чтение config.yaml

# --- База данных ---
psycopg2-binary==2.9.10     # драйвер PostgreSQL (sqlite3 входит в стандартную библиотеку)

# --- Утилиты ---
python-dateutil==2.9.0.post0  # резервный разбор нестандартных дат Jira (основной разбор — стандартный datetime)

# --- Документация (генерация .docx и схемы архитектуры, только для разработки) ---
python-docx==1.1.2          # генерация docs/*.docx
matplotlib==3.9.2           # генерация docs/architecture.png

# --- Тесты (только для разработки) ---
pytest==8.3.4               # запуск юнит-тестов из tests/
pytest-asyncio==0.24.0      # тесты асинхронных корутин
